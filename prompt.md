# Prompt 模板集合

> 以下为 `prompt.txt` 的分类整理版，Prompt 原文原样保留，仅优化排版结构。

---

## 1. 页面生成

```
可以使用AI写前端来生成制作UI 然后可以作前端拆分-拼接的效果：
生成纯前端页面演示以上内容，使用偏暗的炫酷色调，多一些图形效果，
只要有演示的效果即可，其它的网页细节可以忽略，要求1000行以上代码
```

```
生成纯前端页面演示以上内容，使用偏暗的炫酷色调，以xx色为点缀，
多一些图形效果，只要有演示的效果即可，其它的网页细节可以忽略，
(要求1000行以上代码)，(同时使用类似PPT翻页演示的效果)
```

```
生成基于纯前端页面进行演示xxx，使用白色为主色调加以其它炫酷颜色，
以粉红色为点缀，多一些图形效果，只要有演示的效果即可，
其它网页细节可以忽略，要求1000行以上代码
```

```
生成基于纯前端页面进行演示以上内容，以background.html为背景，
多一些图形效果，只要有演示的效果即可，其它的网页细节可以忽略，
要求1000行以上代码
```

```
使用网页前端代码生成一个粉红色的资料卡，
内容是关于"双链路接入"的科普
```

```
生成网页模拟xx(防火墙基于IP地址与端口过滤流量)，
要求完成度高，800行以上代码
```

```
生成网页 xx(防火墙基于IP地址与端口过滤流量)，使用偏暗的色调，
主要注重图形与动态显示效果，在一个屏幕内显示，
要求完成度高，1000行以上代码
(根据以上的代码，给出后续接着的代码)
```

---

## 2. 动画效果

```
现在这个页面中的文字强调效果是文字颜色红色，
优化成从改颜色字体变成给文字加厚涂，
且调整为每次翻页后1.5s再开始逐个进行厚涂的效果。
除了标题厚涂，下面的内容的标红的字也可以加强调效果，使用红色下划线，
而且要按顺序来，先标题厚涂再内容下划线，这样看起来就像真的视频一样。
```

```
为该网页添加一个打开时的加载页面，加载时间为3秒钟
```

```
帮我实现在打开这个网页时，各个元素依次"缓入"出现的动画效果，
且在切换"场景一"与"场景二"时元素内容变化时也添加变化的动画效果
```

```
为文件 xx.html 中的页面内容添加持续动态效果，重点针对页面中的重要文字元素。
实现的动态效果应具有视觉吸引力且不影响内容可读性，
可考虑包括但不限于淡入淡出、平滑滚动、微妙缩放、颜色渐变或文字逐字显示等效果。
确保所有动态效果在不同浏览器和设备上保持一致表现，
添加适当的动画过渡时间控制（建议0.3-0.8秒），
并确保动画效果不影响页面加载性能和用户交互体验。
```

```
实现在打开这个网页时，除了标题和"class="section""的边框与背景默认存在，
其它section内的各个元素依次"缓入"出现的动画效果
```

```
修改各个动画的播放时间，要求整个页面加载完成用时五秒钟
```

```
为网页中的元素添加持续的"强调效果"动画
```

```
为"xx"添加强调的效果(为"网关IP"字段添加强调的效果)
```

```
帮我实现在打开这个网页时，除了右上角的水印外其它各个元素依次"缓入"
出现的动画效果，所有元素出现一共5s
```

```
实现让div class="warning-text"中的文字内容也依次出现
```

```
我发现"ai-arch-diagram"模块内的动画并没有按"人类语言"、
（原始输入箭头）、"词义表征"、（词向量箭头）、"序列建模"（语义理解箭头）、
和"AI 理解"的顺序依次出现
```

```
对文件进行动画效果的精细化优化，要求实现页面中让模块中的各个小元素依次出现
```

```
为虚线箭头元素添加持续循环的流动的动画效果，
使箭头在完成初始出现动画后能够保持动态状态。
```

```
将箭头替换为虚线箭头。为虚线箭头元素添加持续循环的流动的动画效果，
使箭头在完成初始出现动画后能够保持动态状态。
```

```
为所有有鼠标指针hover后发光效果的元素，
设置为默认就有hover的发光效果
```

```
在网页中添加一些颜文字使其看起来很二次元
```

```
hover：在文件 x.html 中，针对x模块下的8个小模块（类似"x"模块），
实现一个自动遍历hover效果的功能。具体要求如下：
配置网页能够按照预设顺序依次对这8个小模块执行鼠标悬停操作，
每个模块的hover状态持续显示1秒后自动切换到下一个模块，
形成循环遍历的效果。
确保hover效果的触发和切换过程平滑自然，
不影响页面其他功能的正常运行。
```

---

## 3. 3D 效果

```
剪映变形：左上 (-500X,500Y)  右上 (0X,0Y)
左下 (0X,0Y)                 右下 (-500X,500Y)
```

```
为该动画添加3D效果
```

```
为X模块整体添加3D旋转效果，实现鼠标往页面左侧移动时该模块Y轴向左，
鼠标往页面右侧移动时该模块Y轴向右
```

```
为X模块整体添加3D旋转效果，实现该模块从Y轴向左15度移动到该模块Y轴向右15度，
每个循环60秒
```

---

## 4. PPT 风格

```
生成基于纯前端页面单页布局仿PPT换页轮播进行直观可视化的xxx介绍
```

```
现要求将每个独立功能模块(模块名称)配置为独占一个完整页面。
实现一个轮播图（PPT）形式的页面切换机制，使各个模块页面能够按照预设顺序
自动轮流播放。系统应包含以下核心功能：
1) 模块页面独立渲染机制，确保每个模块拥有完整的页面空间
2) 轮播控制功能，支持自动播放/暂停、手动上一页/下一页切换
3) 轮播参数配置，可自定义切换动画效果、切换间隔时间、过渡时长
4) 响应式设计，确保在不同设备上均能完整展示模块内容并保持轮播功能正常运行
```

```
(CLINE)根据以上内容生成基于纯前端页面单页布局仿PPT换页轮播
进行直观图形化可视化的介绍，加大文字大小，
并运用加粗、下划线、斜体、文字颜色、文字背景等强调方式方便进行视频演示。
添加每次切换页面时页面中的各个元素依次"缓入"出现的动画效果(细化到每行文字)。
减少PPT演示的字数，增大文字大小，加强图形化显示，图文并茂，方便视频展示。
将emoji图标换成平面ui库的图标。将代码输出到@/9-1.html 中
```

```
(报纸便签风)根据以上内容生成基于纯前端页面单页布局仿PPT换页轮播
进行直观图形化可视化的介绍，加大文字大小，
并运用加粗、下划线、斜体、文字颜色、文字背景等强调方式方便进行视频演示。
添加每次切换页面时页面中的各个元素依次"缓入"出现的动画效果(细化到每行文字)。
减少PPT演示的字数，增大文字大小，加强图形化显示，图文并茂，方便视频展示。
将emoji图标换成平面ui库的图标。
整体页面设计采用报纸、便签风格，将代码输出到 PPT2.html 中
```

```
(学霸笔记)使用前端代码以笔记本/便签的风格，
做一个像一个学霸写满了笔记的笔记本/便签一样的网页演示以上给定的内容，
代码输出到 PPT2-2.html 中
```

```
根据以上内容生成基于纯前端页面单页布局仿PPT换页轮播
进行直观图形化可视化的介绍，加大文字大小，
并运用加粗、下划线、斜体、文字颜色、文字背景等强调方式方便进行视频演示。
添加每次切换页面时页面中的各个元素依次"缓入"出现的动画效果(细化到每行文字)。
```

```
#xx.html 把x页的演示动画重新设计并重做一个
```

```
以页面 Seedance 2.0.html 为模板重构 xxx
```

```
以 PPT-Generate-3.html 为模板演示以上内容
```

```
流程图：以 RNN-2.html 为模板，
将提供的内容恰当地填充进模板网页中，演示以上内容
```

```
以 PPT Template-level2 目录 中的网页为参考演示以下内容，
输出到现存的文件 PPT1.html
```

```
将 x.html 网页的第x和第x页的内容合并后
按照指定图片的平面UI样式进行视觉重构，
保持现有颜色方案不变。具体实施要求包括：
精确还原图片中的布局结构、元素间距、字体样式、图标设计和视觉层次；
确保所有交互元素（按钮、表单、导航等）的视觉表现与参考图片一致；
维持原有的颜色值和配色方案；
保证修改后的页面在不同设备和浏览器上具有良好的响应式表现；
优化DOM结构以匹配UI设计，同时保持原有功能完整性和交互逻辑不变。
完成后需进行视觉一致性检查，确保实现效果与参考图片达到95%以上的相似度。
```

```
减少PPT演示的字数，增大文字大小，加强图形化显示，方便视频展示
```

```
加强PPT页面的图形化演示，图文并茂，方便视频展示，
将PPT的页数限制在5-6页
```

```
增加文字的大小，优化排版，
运用加粗、下划线、斜体、文字颜色、文字背景等强调方式，
使其能更好地在视频上演示
```

```
删除页面右上角的页码，页面下方的页码，
删除页面下方的"← → 键 / 滚轮 / 点击切换"，
将页面上方的进度条也删除掉，但是保留键盘翻页功能
```

```
基于文件 x.html ，设计并开发一个视觉效果类似PPT首页大版报风格的单页网站，
用于展示"xxxxxx"主题内容。页面应包含醒目的标题区域、核心概念简介、
关键特性可视化展示并确保所有内容都保持在一个页面内。
设计需采用分层视觉效果，突出主题重点，使用适合学术主题的配色方案。
页面元素应包括但不限于：大型标题文字、简洁的主题相关内容的示意图、
关键技术特点列表、以及适当的留白设计，
以达到类似PPT首页的视觉冲击力和信息传达效果。
```

```
在文件 x.html 中添加符合二次元风格的颜文字元素，
以增强网页的二次元视觉氛围。
颜文字应与网页内容主题相契合，放置在适当的位置如标题、段落分隔处或交互元素旁。
确保颜文字的添加不会影响网页的正常布局和功能，保持整体视觉协调与美观。
建议选择常见的二次元颜文字类型，并根据页面设计风格调整颜文字的大小和颜色，
使其自然融入网页整体设计。注意不要添加emoji表情符号，
如果需要符号请添加ui库的图标
```

```
PPT1.html 把"大佬开始公开方案"页重新布局：
将 PPT1-codex-config.png 插入这一页
（可选：注意不要删除原页面的元素，只是移动位置)
```

```
PPT1.html "xx"页和"xx"页，基本只有两个卡片和文字在那里，
太无聊且枯燥了，优化一下强化点图形化演示效果
（简单版：第x页，优化一下强化点图形化演示效果）
```

```
我发现 PPT2.html 的质量比起 PPT1.html 有所下降，
优化一下强化图形化演示效果
```

```
PPT1.html 学习一下真正的PPT，
来一点蒙版遮罩、艺术字体、特殊西文字体、3D效果等等好看的效果
```

```
PPT template生成的PPT套RNN流程：
1.以页面 RNN-5.html 为模板重构 PPT-1.html 的"xxx"页的内容，
注意，一定要直接把 PPT-1.html 的"xxx"页的内容套进 RNN-5.html 内，
然后取代原本的"xxx"页
2.为 PPT-1.html 的"xxx"页中的各个元素添加和 RNN-5.html 一样的动画效果，
比如逐个出现的动画、箭头流动动画等
3.(可能的bug) PPT-1.html "xxx"页中各元素的出现动画有个bug，
这个出现动画是在打开 PPT-1.html 的时候就开始播放的，
而不是打开"xxx"页才开始播放的，
导致切换到"xxx"页的时候动画已经播完了元素全都出现了
```

```
#PPTx 让" "页的10个风险都添加鼠标从元素上滑过去时出现下拉的二级菜单，
菜单的内容是对风险的细节描述，
二级菜单的出现(下拉)和消实(上拉)使用延迟曲线函数，
下拉菜单需要在页面层级置顶以防被遮挡
```

```
#PPTx 修改" "页的两个模块，
修改为当用鼠标点击时，在屏幕右侧从右向左滑出一个面板，
面板的内容是点击的模块相关的内容演示等。
```

---

## 5. Trae（工具类）

```
x.html 将"xxx"页使用 ByteByteGo 风格做可视化演示
```

```
继续美化一下，包括排版也可以重新设计，
我觉得现在的主页不够有冲击力，还不够让人眼前一亮，
甚至可以用上一些在前端/设计领域比较顶级前卫的效果
```

```
再强化一下PPT的演示效果，减少一点字数，丰富一下演示/可视化内容，
我觉得现在的演示页面不够有冲击力，还不够让人眼前一亮，
甚至可以用上一些在前端/设计领域比较顶级前卫的效果
```

```
5-1Claude风PPT：PPT1.html ，
多参考 PPT Template-level2 中的网页做一些形象的、复杂的、华丽的图形化效果
```

```
发现变丑了、设计偷懒以后：PPT5.html 重新设计这整个PPT网页，
我对现在的设计整体感到不满意，而且后面的 PPT6.html 和 PPT5.html 有点太像了，
我感觉你有点偷懒，你要参考 PPT Template-level2 里面的设计思路，
多一点发散思维。不要多个PPT采用相同的设计
（我说的不是视觉设计，多个PPT可以用一样的视觉设计，
但是不要每一页都用一样的排版结构和一样的图形演示动画结构ok？），
多发挥点创造力，让人惊喜
```

```
重设这个网页的布局，美化这个网页，使用tailwind样式库
```

```
将 1.html 的背景替换为 background.html 的背景
```

```
修改内容，将内容都压缩到一个屏幕中
```

```
完善这个xxx的页面内容与细节
```

```
将背景与页面风格改为和 #Diffusion.html 一致
```

---

## 6. 美化

```
美化网页内容与可视化效果，添加动态效果，增强网页动态性
```

```
根据现在的内容重构页面，增强图形化效果
```

```
为网页中的元素添加"发光"或"泛光"的效果
```

```
为网页中的元素(除标题外)添加鼠标经过后"强调"的效果
```

```
为页面中一些文字说明改成图文并茂的描述，增强图形化使其演示更形象
```

```
1.png 是一个少女的立绘，我希望把她放到 2.html 的第一个页面中，
将立绘只露出上半部分，但是上半部分放大到能填充整个页面的高度位置。
确保添加了人物之后不会影响原本页面的翻页功能
```

```
(加表情包)在" 超时空辉夜姬表情包 "文件夹中包含以情绪命名的GIF表情包合集，
需要根据表情包的情绪名称选择合适的表情包
插入到页面 Note1 emoji.html 的内容部分。具体要求如下：
1. 插入数量控制在2-5个之间（包含2个和5个）；
2. 必须保持页面原有的布局结构不变，不得对现有页面布局做任何修改；
3. 插入的表情包图片不得遮挡页面现有内容；
4. 需根据页面内容的上下文语境，选择情绪匹配度最高的表情包进行插入；
5. 确保插入的表情包与周围文本内容自然融合，
不影响页面的整体阅读体验和视觉美感。
```

---

## 7. UI 置换（附图参考）

```
将 x.html 网页按照指定图片的平面UI样式进行视觉重构，
保持现有颜色方案不变。具体实施要求包括：
精确还原图片中的布局结构、元素间距、字体样式、图标设计和视觉层次；
确保所有交互元素（按钮、表单、导航等）的视觉表现与参考图片一致；
维持原有的颜色值和配色方案；保证修改后的页面在不同设备和浏览器上
具有良好的响应式表现；优化DOM结构以匹配UI设计，同时保持原有功能完整性和交互逻辑不变。
完成后需进行视觉一致性检查，确保实现效果与参考图片达到95%以上的相似度。
```

```
将 x.html 网页的第六页、第七页、第八页和第九页的内容合并后
按照指定图片的平面UI样式进行视觉重构，保持现有颜色方案不变。
具体实施要求包括：
精确还原图片中的布局结构、元素间距、字体样式、图标设计和视觉层次；
确保所有交互元素（按钮、表单、导航等）的视觉表现与参考图片一致；
维持原有的颜色值和配色方案；保证修改后的页面在不同设备和浏览器上
具有良好的响应式表现；优化DOM结构以匹配UI设计，同时保持原有功能完整性和交互逻辑不变。
完成后需进行视觉一致性检查，确保实现效果与参考图片达到95%以上的相似度。
将重构的页面保存到 xx.html
```

```
将网页修改为图片中的平面ui设计样式(配色不变)
```

```
将emoji图标换成平面ui库的图标
```
